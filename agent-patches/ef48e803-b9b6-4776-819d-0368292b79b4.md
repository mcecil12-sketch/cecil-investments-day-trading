# Agent Patch ef48e803-b9b6-4776-819d-0368292b79b4

## Title
Zero-signal guardrail: force-post top candidates in recovery mode

## Summary
[ZERO SIGNAL DAY GUARDRAIL] Market has been open 23 minutes with 0 signals received.

Required actions:
1. Force-post top 1–2 candidates regardless of soft filter thresholds
2. Mark signals as recovery_mode=true to bypass quality gates
3. Relax relVol threshold by 10–20% (e.g. minRelVol * 0.8)
4. Log forcedPostFallback=true on every force-posted signal
5. Target: get at least 1 signal into the store within next scan cycle

Blocked reason: 48 scan(s) ran but 0 signals posted — candidates may be rejected by pre-post gating or quality filters

## Copilot Prompt
Force at least 1 signal into the store to unblock the zero-signal day

## Patch Plan Summary
[ZERO SIGNAL DAY GUARDRAIL] Market has been open 23 minutes with 0 signals received.

Required actions:
1. Force-post top 1–2 candidates regardless of soft filter thresholds
2. Mark signals as recovery_mode=true to bypass quality gates
3. Relax relVol threshold by 10–20% (e.g. minRelVol * 0.8)
4. Log forcedPostFallback=true on every force-posted signal
5. Target: get at least 1 signal into the store within next scan cycle

Blocked reason: 48 scan(s) ran but 0 signals posted — candidates may be rejected by pre-post gating or quality filters

## Patch Targets
- app/api/scan/route.ts
- app/api/signals/route.ts
- lib/signals/since.ts

## Validation Plan
- buildRequired: true
- testCommands: (none)
- smokeChecks: /api/scan | /api/signals | /api/funnel-health

## Commit Plan
- commitMessage: agent: Zero-signal guardrail: force-post top candidates in recovery mode
- targetBranch: main
- pushDirect: true

## Generated At
2026-06-09T17:11:09.498Z
