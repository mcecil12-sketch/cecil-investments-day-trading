# Agent Patch be0ea9f2-0b6a-4756-9739-a9ab5365320c

## Title
Fix scan→signal posting failure: relax thresholds + force-post fallback

## Summary
[SCAN→SIGNAL POSTING FAILURE] 3 candidate(s) found by scanner but 0 signals posted.

Required actions:
1. Relax relVol threshold by 10–20% (multiply current minRelVol threshold by 0.8)
2. Relax trend strength requirement slightly (reduce minTrendScore by 0.1)
3. Allow fallback posting for top 1–3 candidates when all soft filters reject
4. Set forcedPostFallback=true on fallback-posted signals for audit trail
5. Ensure fallback path does NOT bypass hard risk limits (minPrice, volatility caps)

Blocked reason: 48 scan(s) ran but 0 signals posted — candidates may be rejected by pre-post gating or quality filters

## Copilot Prompt
Unblock scan→signal pipeline with threshold relaxation + force-post fallback

## Patch Plan Summary
[SCAN→SIGNAL POSTING FAILURE] 3 candidate(s) found by scanner but 0 signals posted.

Required actions:
1. Relax relVol threshold by 10–20% (multiply current minRelVol threshold by 0.8)
2. Relax trend strength requirement slightly (reduce minTrendScore by 0.1)
3. Allow fallback posting for top 1–3 candidates when all soft filters reject
4. Set forcedPostFallback=true on fallback-posted signals for audit trail
5. Ensure fallback path does NOT bypass hard risk limits (minPrice, volatility caps)

Blocked reason: 48 scan(s) ran but 0 signals posted — candidates may be rejected by pre-post gating or quality filters

## Patch Targets
- app/api/scan/route.ts
- lib/scanner/prePostGating.ts
- app/api/signals/route.ts

## Validation Plan
- buildRequired: true
- testCommands: (none)
- smokeChecks: /api/scan | /api/signals | /api/signals/all

## Commit Plan
- commitMessage: agent: Fix scan→signal posting failure: relax thresholds + force-post fallback
- targetBranch: main
- pushDirect: true

## Generated At
2026-05-11T21:25:49.458Z
