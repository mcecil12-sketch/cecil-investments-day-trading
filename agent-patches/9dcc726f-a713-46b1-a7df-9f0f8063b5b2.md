# Agent Patch 9dcc726f-a713-46b1-a7df-9f0f8063b5b2

## Title
Production Incident — Scoring Bottleneck v2

## Summary
Scoring throughput is below signal intake causing backlog growth. Drain loop only iterates once, timeout rate is too high, and backlog is not decreasing meaningfully during market hours.

## Copilot Prompt
Scoring throughput is below signal intake causing backlog growth. Drain loop only iterates once, timeout rate is too high, and backlog is not decreasing meaningfully during market hours.

## Patch Plan Summary
Scoring throughput is below signal intake causing backlog growth. Drain loop only iterates once, timeout rate is too high, and backlog is not decreasing meaningfully during market hours.

## Patch Targets
- app/api/ai/score/drain/route.ts
- app/api/signals/all/route.ts
- app/api/readiness/route.ts
- lib/agents/verification-runner.ts
- lib/agents/manual-action-queue.ts

## Validation Plan
- buildRequired: true
- testCommands: (none)
- smokeChecks: /api/ai/score/drain | /api/signals/all | /api/readiness | /api/scan/health

## Commit Plan
- commitMessage: agent: Production Incident — Scoring Bottleneck v2
- targetBranch: main
- pushDirect: true

## Generated At
2026-04-13T20:10:52.968Z
