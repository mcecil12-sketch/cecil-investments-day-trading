# Agent Patch 59992ed2-e48c-4af8-9722-8bdc9213e63b

## Title
Fix public agent intake to support message-only conversational payloads

## Summary
The GPT Action relay is live, but conversational task submission still fails because the intake path enforces a required top-level title before conversational parsing completes. Update the public relay and canonical conversational intake flow so a payload with only message is valid, and title is derived from the parsed conversational body instead of required as a separate top-level field.

## Copilot Prompt
The GPT Action relay is live, but conversational task submission still fails because the intake path enforces a required top-level title before conversational parsing completes. Update the public relay and canonical conversational intake flow so a payload with only message is valid, and title is derived from the parsed conversational body instead of required as a separate top-level field.

## Patch Plan Summary
The GPT Action relay is live, but conversational task submission still fails because the intake path enforces a required top-level title before conversational parsing completes. Update the public relay and canonical conversational intake flow so a payload with only message is valid, and title is derived from the parsed conversational body instead of required as a separate top-level field.

## Patch Targets
- app/api/agents/chat-intake-public/route.ts
- app/api/agents/chat-intake/route.ts
- app/api/agents/intake/route.ts
- lib/agents/*
- lib/agents/manual-action-queue.ts

## Validation Plan
- buildRequired: true
- testCommands: (none)
- smokeChecks: /api/agents/chat-intake-public | /api/agents/chat-intake | /api/agents/intake

## Commit Plan
- commitMessage: agent: Fix public agent intake to support message-only conversational payloads
- targetBranch: main
- pushDirect: true

## Generated At
2026-04-16T01:58:41.375Z
